import Input from "./Input";
  
const meta = {
    component: Input,
    title: 'Components/Atoms/Input',
    argTypes: {
    }
}

export default meta;

export const Primary = {
    args: {
        placeholder: 'text를 입력해 주세요'
    }
}