import Text from "./Text";

const meta = {
    component: Text,
    title: 'Components/Atoms/Text',
    argTypes: {
    }
}

export default meta;

export const Default = {
    args: {
        color: 'default',
        text: 'default text'
    }
}